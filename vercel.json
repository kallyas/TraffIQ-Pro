{
  "buildCommand": "pnpm --filter ./frontend build && rm -rf dist && cp -R frontend/dist dist",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
